# Build/run artifacts of the LTP lane — regenerated by build.sh/run.sh.
# The lane is reproducible from the pinned commit in build.sh, so nothing here
# is vendored: the fetched LTP checkout, compiled binaries, logs and results.
out/

//! Behavioural tests for the runtime surface: the export census, the device model, and the memory and
//! synchronization paths driven against a real in-process executor over the socket transport.

mod census;
mod device;
mod memory;
mod support;

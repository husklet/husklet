[package]
name = "hl-client"
version = "0.4.0"
edition = "2021"
description = "Docker-Engine-API client library for hl-daemon — a thin bollard façade with hl-specific view models, shared by the hl CLI and GUI."
license = "MIT"

[lib]
name = "hl_client"
path = "src/lib.rs"

[dependencies]
# Docker-Engine-API client over a unix socket (mature wire transport) + the stream/log plumbing.
bollard = "0.21"
futures-util = { version = "0.3", default-features = false, features = ["std"] }
bytes = "1"

image: alpine:3.20
execution: { native: true, diagnostics: true }
workload:
  rootfs: { executable: /bin/true }
phases: [create, attach, start, wait-and-drain, output-read]
cases:
  - id: lifecycle
    warmups: 1
    samples: 11
    expect: { stdout_contains: golden/empty.txt }

cases:
- id: terminal-million-lines/tail-and-responsive-shell
  image: bash:5.2
  resources: [pty]
  timeout: 120
  actions:
  - terminal:
      argv: [/usr/local/bin/bash, --noprofile, --norc]
      rows: 24
      columns: 80
      steps:
      - await_output: { contains: "bash-5.2#", timeout_ms: 5000 }
      - write: { text: "stty -echo; seq 1 1000000; printf '<MILLION-DONE>\\n'\r" }
      - await_output: { contains: "<MILLION-DONE>", timeout_ms: 60000 }
      - write: { text: "printf '<AFTER-FLOOD>\\n'; exit\r" }
      - await_output: { contains: "<AFTER-FLOOD>", timeout_ms: 5000 }
      - close: {}
  expect:
    stdout_contains:
    - golden/tail.txt
    - golden/done.txt
    - golden/responsive.txt

// @ts-nocheck -- legacy story typing is migrated incrementally.
import React from 'react'; import { Button, Column, DependencyCycle, DependencyCycleMember, DependencyEdge, DependencyGraph, DependencyNode, Heading, InlineMessage, Row, Text } from '@husklet/react';
const {createElement:h,useState}=React; export const DEPENDENCY_GRAPH_STORY='Dependency graph'; export const NODE_LIMIT=32,EDGE_LIMIT=128,CYCLE_LIMIT=8,MEMBER_LIMIT=6; const clean=(v,n)=>String(v).replace(/[\u0000-\u001f\u007f-\u009f]/g,' ').slice(0,n);
export function boundedGraph(graph){const nodes=graph.nodes.slice(0,NODE_LIMIT).map(n=>({...n,id:clean(n.id,40),label:clean(n.label,120),version:clean(n.version,64),detail:clean(n.detail,160)}));const ids=new Set(nodes.map(n=>n.id));const edges=graph.edges.filter(e=>ids.has(e.source)&&ids.has(e.target)).slice(0,EDGE_LIMIT);const cycles=graph.cycles.filter(c=>c.length&&c.length<=MEMBER_LIMIT&&c.every(id=>ids.has(id))&&c.every((id,i)=>edges.some(e=>e.source===id&&e.target===c[(i+1)%c.length]))).slice(0,CYCLE_LIMIT);return {nodes,edges,cycles,totals:graph.totals}}
const data=boundedGraph({nodes:[{id:'app',label:'app',version:'1.0',state:'resolved',detail:'root'},{id:'react',label:'react',version:'18.3',state:'conflict',detail:'18 and 19 requested'},{id:'scheduler',label:'scheduler',version:'0.23',state:'missing',detail:'peer absent'}],edges:[{source:'app',target:'react',relation:'runtime',requirement:'^18'},{source:'react',target:'scheduler',relation:'peer',requirement:'^0.23'},{source:'scheduler',target:'react',relation:'runtime',requirement:'*'}],cycles:[['react','scheduler']],totals:{nodes:87,edges:240,cycles:2}});
export function DependencyGraphStory(){const[issues,setIssues]=useState(false);const nodes=issues?data.nodes.filter(n=>n.state!=='resolved'):data.nodes;const ids=new Set(nodes.map(n=>n.id));return h(Column,{gap:2,grow:true},h(Heading,{label:'Dependency graph',scale:'title'}),h(Text,{label:'Inspect resolved packages, cross-dependencies, and verified cycles.'}),h(Row,{gap:1},h(Button,{label:issues?'Show all':'Show issues only',onInvoke:()=>setIssues(!issues)})),h(DependencyGraph,{label:`${nodes.length} dependencies`,detail:`bounded source: nodes ${nodes.length}/${data.totals.nodes}, edges ${data.edges.length}/${data.totals.edges}, cycles ${data.cycles.length}/${data.totals.cycles}`},...nodes.map(n=>h(DependencyNode,{key:n.id,label:`${n.label}@${n.version}`,value:`id=${n.id} state=${n.state} detail=${n.detail}`},...data.edges.filter(e=>e.source===n.id&&ids.has(e.target)).map((e,i)=>h(DependencyEdge,{key:i,label:`${e.relation} → ${e.target}`,value:`requirement=${e.requirement}`})))),...data.cycles.map((c,i)=>h(DependencyCycle,{key:`c${i}`,label:`cycle ${i+1}`,detail:`${c.length} members`},...c.map((m,j)=>h(DependencyCycleMember,{key:j,label:m,value:`position=${j}`}))))),h(InlineMessage,{label:'Explicit bounded-source totals remain visible after filtering.'}))}

// Generated by tools/types.js from catalogue.json. Do not edit.
//
// One interface per component, carrying the properties that component declares
// and the handlers for the interactions it can report. A property no component
// declares is a type error, which is the whole point of generating this.

import type { ReactNode } from 'react';

/** A spacing or sizing quantity: steps on the 4px scale unless named. */
export type Length = number | 'fill' | 'content' | { chars: number } | { step: number };

/** Space inside a component's own edges, per side or all at once. */
export type Edges = Length | { top?: Length; end?: Length; bottom?: Length; start?: Length };

/** An exact extent, or a floor and a ceiling. */
export type Bounds = Length | { minimum?: Length; maximum?: Length };

/** One selectable option offered by a choice control. */
export interface Choice {
  value: string;
  label: string;
}

/** One declared column of a table. */
export interface ColumnSpec {
  key: string;
  title?: string;
  width?: Length;
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  sortable?: boolean;
}

/** One interaction, as the host reports it. */
export interface Report {
  trigger: string;
  node: number;
  id: string;
  value: unknown;
}

/** What every component takes, whatever it is. */
export interface NodeProps {
  key?: string | number | null;
}


export interface ColumnProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** axis of a container, a splitter or a divider */
  orientation?: "horizontal" | "Horizontal" | "vertical" | "Vertical";
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
}

export interface RowProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** axis of a container, a splitter or a divider */
  orientation?: "horizontal" | "Horizontal" | "vertical" | "Vertical";
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
}

export interface GridProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** grid column count, or a table's declared column count; never below one */
  columns?: number;
}

export interface ScrollProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
}

export interface SplitterProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** axis of a container, a splitter or a divider */
  orientation?: "horizontal" | "Horizontal" | "vertical" | "Vertical";
  /** the divider position of a splitter, in pixels */
  position?: number;
}

export interface StackProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
}

export interface OverlayProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
}

export interface ContainerProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface SpacerProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
}

export interface SeparatorProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** axis of a container, a splitter or a divider */
  orientation?: "horizontal" | "Horizontal" | "vertical" | "Vertical";
}

export interface CardProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface CardHeaderProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** secondary text beside the label, or a tooltip where there is no room */
  detail?: string;
  /** named icon */
  icon?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface CardContentProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface CardActionsProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface CardMediaProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the address a link points at, or the file a picture or a video plays */
  uri?: string;
}

export interface CardActionAreaProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface PaperProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface SectionProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface ToolbarProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface HeaderBarProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
}

export interface SidebarProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface TextProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
  /** for a label */
  ellipsize?: boolean;
  /** text size role */
  scale?: "caption" | "Caption" | "body" | "Body" | "title" | "Title" | "display" | "Display";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  /** a semantic color slot, never a literal color */
  color?: "ground" | "Ground" | "surface" | "Surface" | "raised" | "Raised" | "line" | "Line" | "text" | "Text" | "text-dim" | "TextDim" | "text-faint" | "TextFaint" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger" | "info" | "Info";
}

export interface HeadingProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
  /** for a label */
  ellipsize?: boolean;
  /** text size role */
  scale?: "caption" | "Caption" | "body" | "Body" | "title" | "Title" | "display" | "Display";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  /** a semantic color slot, never a literal color */
  color?: "ground" | "Ground" | "surface" | "Surface" | "raised" | "Raised" | "line" | "Line" | "text" | "Text" | "text-dim" | "TextDim" | "text-faint" | "TextFaint" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger" | "info" | "Info";
}

export interface CodeProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
  /** for a label */
  ellipsize?: boolean;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  /** a semantic color slot, never a literal color */
  color?: "ground" | "Ground" | "surface" | "Surface" | "raised" | "Raised" | "line" | "Line" | "text" | "Text" | "text-dim" | "TextDim" | "text-faint" | "TextFaint" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger" | "info" | "Info";
}

export interface LinkProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** the address a link points at, or the file a picture or a video plays */
  uri?: string;
  /** named icon */
  icon?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface IconProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** named icon */
  icon?: string;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  /** a semantic color slot, never a literal color */
  color?: "ground" | "Ground" | "surface" | "Surface" | "raised" | "Raised" | "line" | "Line" | "text" | "Text" | "text-dim" | "TextDim" | "text-faint" | "TextFaint" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger" | "info" | "Info";
}

export interface BadgeProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
}

export interface AvatarProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface AvatarGroupProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface ChipProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
}

export interface ImageProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the address a link points at, or the file a picture or a video plays */
  uri?: string;
}

export interface ImageListProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** grid column count, or a table's declared column count; never below one */
  columns?: number;
}

export interface ImageListItemProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the address a link points at, or the file a picture or a video plays */
  uri?: string;
}

export interface ProgressProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** progress or meter fill, clamped to nought through one */
  fraction?: number;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface SpinnerProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** runs a spinner; defaults to busy when absent */
  busy?: boolean;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface MeterProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** progress or meter fill, clamped to nought through one */
  fraction?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface SkeletonProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
}

export interface EmptyStateProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** secondary text beside the label, or a tooltip where there is no room */
  detail?: string;
  /** named icon */
  icon?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface StatProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** the node's caption */
  label?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface ToastProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** for an expander or a revealed panel */
  expanded?: boolean;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
}

export interface BannerProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** for an expander or a revealed panel */
  expanded?: boolean;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
}

export interface AlertTitleProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** text size role */
  scale?: "caption" | "Caption" | "body" | "Body" | "title" | "Title" | "display" | "Display";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface InlineMessageProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface ButtonProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface IconButtonProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** named icon */
  icon?: string;
  /** the node's caption */
  label?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface ToggleButtonProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** for a check button, a toggle button or a switch */
  checked?: boolean;
  /** read like Checked */
  selected?: boolean;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onToggle?: (report: Report) => void;
  onInvoke?: (report: Report) => void;
}

export interface ButtonGroupProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** axis of a container, a splitter or a divider */
  orientation?: "horizontal" | "Horizontal" | "vertical" | "Vertical";
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
}

export interface ToggleButtonGroupProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** axis of a container, a splitter or a divider */
  orientation?: "horizontal" | "Horizontal" | "vertical" | "Vertical";
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
}

export interface SplitButtonProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface FabProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** named icon */
  icon?: string;
  /** the node's caption */
  label?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface SpeedDialProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** named icon */
  icon?: string;
  /** the node's caption */
  label?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
}

export interface SpeedDialActionProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface OverflowProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** named icon */
  icon?: string;
  /** the node's caption */
  label?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
}

export interface EntryProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** prompt shown by an empty field */
  placeholder?: string;
  /** hides typed text, and on a password entry withholds the peek icon */
  secret?: boolean;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onChange?: (report: Report) => void;
}

export interface SearchProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** prompt shown by an empty field */
  placeholder?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  onChange?: (report: Report) => void;
}

export interface NumberEntryProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** lower bound of a slider or a number entry */
  minimum?: number;
  /** upper bound of a slider or a number entry */
  maximum?: number;
  /** increment of a slider or a number entry */
  step?: number;
  /** defaults to enabled when absent */
  enabled?: boolean;
  onChange?: (report: Report) => void;
}

export interface TextAreaProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** for a text view; defaults to monospace when absent */
  monospace?: boolean;
  /** defaults to enabled when absent */
  enabled?: boolean;
}

export interface PasswordEntryProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** prompt shown by an empty field */
  placeholder?: string;
  /** hides typed text, and on a password entry withholds the peek icon */
  secret?: boolean;
  /** defaults to enabled when absent */
  enabled?: boolean;
  onChange?: (report: Report) => void;
}

export interface AutocompleteProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** options of a select or a radio group, each a value and a label */
  choices?: Choice[];
  /** defaults to enabled when absent */
  enabled?: boolean;
  onChange?: (report: Report) => void;
}

export interface TextFieldProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** secondary text beside the label, or a tooltip where there is no room */
  detail?: string;
  /** prompt shown by an empty field */
  placeholder?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** defaults to enabled when absent */
  enabled?: boolean;
}

export interface InputAdornmentProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface SliderProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** lower bound of a slider or a number entry */
  minimum?: number;
  /** upper bound of a slider or a number entry */
  maximum?: number;
  /** increment of a slider or a number entry */
  step?: number;
  /** defaults to enabled when absent */
  enabled?: boolean;
  onChange?: (report: Report) => void;
}

export interface DatePickerProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** defaults to enabled when absent */
  enabled?: boolean;
}

export interface TimePickerProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** defaults to enabled when absent */
  enabled?: boolean;
}

export interface ColorPickerProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** defaults to enabled when absent */
  enabled?: boolean;
}

export interface FilePickerProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface RatingProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** defaults to enabled when absent */
  enabled?: boolean;
  onChange?: (report: Report) => void;
}

export interface FormControlProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface FormLabelProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface FormHelperTextProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface FormControlLabelProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface FormGroupProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface SwitchProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** for a check button, a toggle button or a switch */
  checked?: boolean;
  /** read like Checked */
  selected?: boolean;
  /** defaults to enabled when absent */
  enabled?: boolean;
  onToggle?: (report: Report) => void;
}

export interface CheckboxProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** for a check button, a toggle button or a switch */
  checked?: boolean;
  /** read like Checked */
  selected?: boolean;
  /** defaults to enabled when absent */
  enabled?: boolean;
  onToggle?: (report: Report) => void;
}

export interface RadioProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** for a check button, a toggle button or a switch */
  checked?: boolean;
  /** read like Checked */
  selected?: boolean;
  /** defaults to enabled when absent */
  enabled?: boolean;
  onToggle?: (report: Report) => void;
}

export interface RadioGroupProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** options of a select or a radio group, each a value and a label */
  choices?: Choice[];
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** axis of a container, a splitter or a divider */
  orientation?: "horizontal" | "Horizontal" | "vertical" | "Vertical";
}

export interface SelectProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** options of a select or a radio group, each a value and a label */
  choices?: Choice[];
  /** defaults to enabled when absent */
  enabled?: boolean;
  onChange?: (report: Report) => void;
}

export interface ListProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
}

export interface ListRowProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface ListItemTextProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** secondary text beside the label, or a tooltip where there is no room */
  detail?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface ListItemIconProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** named icon */
  icon?: string;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface ListItemAvatarProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface ListItemButtonProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface ListItemActionProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface ListItemSecondaryActionProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface ListSubheaderProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** text size role */
  scale?: "caption" | "Caption" | "body" | "Body" | "title" | "Title" | "display" | "Display";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface TableProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface TableHeadProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface TableBodyProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface TableFooterProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface TableRowProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface TableCellProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** for a label */
  ellipsize?: boolean;
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface TableSortLabelProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface DataTableProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** table columns: key, title, width as a Length, align, sortable */
  schema?: ColumnSpec[];
  /** identity of the windowed row source backing a collection */
  source?: number;
}

export interface TreeTableProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** table columns: key, title, width as a Length, align, sortable */
  schema?: ColumnSpec[];
  /** identity of the windowed row source backing a collection */
  source?: number;
}

export interface TablePaginationProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** the node's caption */
  label?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface TreeProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
}

export interface TreeItemProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** for an expander or a revealed panel */
  expanded?: boolean;
  onExpand?: (report: Report) => void;
}

export interface TabsProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
}

export interface TabPageProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface BreadcrumbProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** axis of a container, a splitter or a divider */
  orientation?: "horizontal" | "Horizontal" | "vertical" | "Vertical";
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
}

export interface PaginationProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** axis of a container, a splitter or a divider */
  orientation?: "horizontal" | "Horizontal" | "vertical" | "Vertical";
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
}

export interface PaginationItemProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface StepperProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
  /** axis of a container, a splitter or a divider */
  orientation?: "horizontal" | "Horizontal" | "vertical" | "Vertical";
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
}

export interface StepProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface StepLabelProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface StepContentProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface StepConnectorProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** axis of a container, a splitter or a divider */
  orientation?: "horizontal" | "Horizontal" | "vertical" | "Vertical";
}

export interface StepIconProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** named icon */
  icon?: string;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface NavigationRailProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface NavigationRailItemProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface BottomNavigationProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface BottomNavigationActionProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface AccordionProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** for an expander or a revealed panel */
  expanded?: boolean;
  onExpand?: (report: Report) => void;
}

export interface AccordionSummaryProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface AccordionDetailsProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface AccordionActionsProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface ExpanderProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** for an expander or a revealed panel */
  expanded?: boolean;
  onExpand?: (report: Report) => void;
}

export interface DialogProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface DialogTitleProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** text size role */
  scale?: "caption" | "Caption" | "body" | "Body" | "title" | "Title" | "display" | "Display";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface DialogContentProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface DialogContentTextProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** words onto further lines for text, whole children for a row or a column */
  wrap?: boolean;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}

export interface DialogActionsProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface PopoverProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
}

export interface ContextMenuProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
}

export interface MenuProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface MenuItemProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** named icon */
  icon?: string;
  /** defaults to enabled when absent */
  enabled?: boolean;
  /** emphasis level */
  variant?: "plain" | "Plain" | "filled" | "Filled" | "outline" | "Outline" | "ghost" | "Ghost";
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
  onInvoke?: (report: Report) => void;
}

export interface DrawerProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
}

export interface DrawerPanelProps extends NodeProps {
  children?: ReactNode;
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** for an expander or a revealed panel */
  expanded?: boolean;
  /** space between children; only a Step length has a pixel size */
  gap?: Length;
}

export interface CodeViewProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** for a text view; defaults to monospace when absent */
  monospace?: boolean;
}

export interface LogViewProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** what a field holds or a display shows; numeric for a slider, a number entry and a rating */
  value?: string | number;
  /** for a text view; defaults to monospace when absent */
  monospace?: boolean;
}

export interface VideoProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the address a link points at, or the file a picture or a video plays */
  uri?: string;
}

export interface ChartProps extends NodeProps {
  /** defaults to visible when absent */
  visible?: boolean;
  /** explanation revealed by a pointer */
  tooltip?: string;
  /** an exact extent, or a floor and a ceiling */
  width?: Length | Bounds;
  /** an exact extent, or a floor and a ceiling */
  height?: Length | Bounds;
  /** a Length applies to all four sides; Edges names them separately */
  pad?: Length | Edges;
  /** placement along the main axis */
  align?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** placement along the cross axis */
  justify?: "start" | "Start" | "center" | "Center" | "end" | "End" | "stretch" | "Stretch";
  /** any value above zero expands the child on both axes */
  grow?: number | boolean;
  /** grid columns this child occupies; never below one */
  span?: number;
  /** grid rows this child occupies; never below one */
  rowSpan?: number;
  /** the node's caption */
  label?: string;
  /** semantic weight */
  tone?: "neutral" | "Neutral" | "accent" | "Accent" | "positive" | "Positive" | "warning" | "Warning" | "danger" | "Danger";
}


import type { ComponentType } from 'react';

export const Column: ComponentType<ColumnProps>;
export const Row: ComponentType<RowProps>;
export const Grid: ComponentType<GridProps>;
export const Scroll: ComponentType<ScrollProps>;
export const Splitter: ComponentType<SplitterProps>;
export const Stack: ComponentType<StackProps>;
export const Overlay: ComponentType<OverlayProps>;
export const Container: ComponentType<ContainerProps>;
export const Spacer: ComponentType<SpacerProps>;
export const Separator: ComponentType<SeparatorProps>;
export const Card: ComponentType<CardProps>;
export const CardHeader: ComponentType<CardHeaderProps>;
export const CardContent: ComponentType<CardContentProps>;
export const CardActions: ComponentType<CardActionsProps>;
export const CardMedia: ComponentType<CardMediaProps>;
export const CardActionArea: ComponentType<CardActionAreaProps>;
export const Paper: ComponentType<PaperProps>;
export const Section: ComponentType<SectionProps>;
export const Toolbar: ComponentType<ToolbarProps>;
export const HeaderBar: ComponentType<HeaderBarProps>;
export const Sidebar: ComponentType<SidebarProps>;
export const Text: ComponentType<TextProps>;
export const Heading: ComponentType<HeadingProps>;
export const Code: ComponentType<CodeProps>;
export const Link: ComponentType<LinkProps>;
export const Icon: ComponentType<IconProps>;
export const Badge: ComponentType<BadgeProps>;
export const Avatar: ComponentType<AvatarProps>;
export const AvatarGroup: ComponentType<AvatarGroupProps>;
export const Chip: ComponentType<ChipProps>;
export const Image: ComponentType<ImageProps>;
export const ImageList: ComponentType<ImageListProps>;
export const ImageListItem: ComponentType<ImageListItemProps>;
export const Progress: ComponentType<ProgressProps>;
export const Spinner: ComponentType<SpinnerProps>;
export const Meter: ComponentType<MeterProps>;
export const Skeleton: ComponentType<SkeletonProps>;
export const EmptyState: ComponentType<EmptyStateProps>;
export const Stat: ComponentType<StatProps>;
export const Toast: ComponentType<ToastProps>;
export const Banner: ComponentType<BannerProps>;
export const AlertTitle: ComponentType<AlertTitleProps>;
export const InlineMessage: ComponentType<InlineMessageProps>;
export const Button: ComponentType<ButtonProps>;
export const IconButton: ComponentType<IconButtonProps>;
export const ToggleButton: ComponentType<ToggleButtonProps>;
export const ButtonGroup: ComponentType<ButtonGroupProps>;
export const ToggleButtonGroup: ComponentType<ToggleButtonGroupProps>;
export const SplitButton: ComponentType<SplitButtonProps>;
export const Fab: ComponentType<FabProps>;
export const SpeedDial: ComponentType<SpeedDialProps>;
export const SpeedDialAction: ComponentType<SpeedDialActionProps>;
export const Overflow: ComponentType<OverflowProps>;
export const Entry: ComponentType<EntryProps>;
export const Search: ComponentType<SearchProps>;
export const NumberEntry: ComponentType<NumberEntryProps>;
export const TextArea: ComponentType<TextAreaProps>;
export const PasswordEntry: ComponentType<PasswordEntryProps>;
export const Autocomplete: ComponentType<AutocompleteProps>;
export const TextField: ComponentType<TextFieldProps>;
export const InputAdornment: ComponentType<InputAdornmentProps>;
export const Slider: ComponentType<SliderProps>;
export const DatePicker: ComponentType<DatePickerProps>;
export const TimePicker: ComponentType<TimePickerProps>;
export const ColorPicker: ComponentType<ColorPickerProps>;
export const FilePicker: ComponentType<FilePickerProps>;
export const Rating: ComponentType<RatingProps>;
export const FormControl: ComponentType<FormControlProps>;
export const FormLabel: ComponentType<FormLabelProps>;
export const FormHelperText: ComponentType<FormHelperTextProps>;
export const FormControlLabel: ComponentType<FormControlLabelProps>;
export const FormGroup: ComponentType<FormGroupProps>;
export const Switch: ComponentType<SwitchProps>;
export const Checkbox: ComponentType<CheckboxProps>;
export const Radio: ComponentType<RadioProps>;
export const RadioGroup: ComponentType<RadioGroupProps>;
export const Select: ComponentType<SelectProps>;
export const List: ComponentType<ListProps>;
export const ListRow: ComponentType<ListRowProps>;
export const ListItemText: ComponentType<ListItemTextProps>;
export const ListItemIcon: ComponentType<ListItemIconProps>;
export const ListItemAvatar: ComponentType<ListItemAvatarProps>;
export const ListItemButton: ComponentType<ListItemButtonProps>;
export const ListItemAction: ComponentType<ListItemActionProps>;
export const ListItemSecondaryAction: ComponentType<ListItemSecondaryActionProps>;
export const ListSubheader: ComponentType<ListSubheaderProps>;
export const Table: ComponentType<TableProps>;
export const TableHead: ComponentType<TableHeadProps>;
export const TableBody: ComponentType<TableBodyProps>;
export const TableFooter: ComponentType<TableFooterProps>;
export const TableRow: ComponentType<TableRowProps>;
export const TableCell: ComponentType<TableCellProps>;
export const TableSortLabel: ComponentType<TableSortLabelProps>;
export const DataTable: ComponentType<DataTableProps>;
export const TreeTable: ComponentType<TreeTableProps>;
export const TablePagination: ComponentType<TablePaginationProps>;
export const Tree: ComponentType<TreeProps>;
export const TreeItem: ComponentType<TreeItemProps>;
export const Tabs: ComponentType<TabsProps>;
export const TabPage: ComponentType<TabPageProps>;
export const Breadcrumb: ComponentType<BreadcrumbProps>;
export const Pagination: ComponentType<PaginationProps>;
export const PaginationItem: ComponentType<PaginationItemProps>;
export const Stepper: ComponentType<StepperProps>;
export const Step: ComponentType<StepProps>;
export const StepLabel: ComponentType<StepLabelProps>;
export const StepContent: ComponentType<StepContentProps>;
export const StepConnector: ComponentType<StepConnectorProps>;
export const StepIcon: ComponentType<StepIconProps>;
export const NavigationRail: ComponentType<NavigationRailProps>;
export const NavigationRailItem: ComponentType<NavigationRailItemProps>;
export const BottomNavigation: ComponentType<BottomNavigationProps>;
export const BottomNavigationAction: ComponentType<BottomNavigationActionProps>;
export const Accordion: ComponentType<AccordionProps>;
export const AccordionSummary: ComponentType<AccordionSummaryProps>;
export const AccordionDetails: ComponentType<AccordionDetailsProps>;
export const AccordionActions: ComponentType<AccordionActionsProps>;
export const Expander: ComponentType<ExpanderProps>;
export const Dialog: ComponentType<DialogProps>;
export const DialogTitle: ComponentType<DialogTitleProps>;
export const DialogContent: ComponentType<DialogContentProps>;
export const DialogContentText: ComponentType<DialogContentTextProps>;
export const DialogActions: ComponentType<DialogActionsProps>;
export const Popover: ComponentType<PopoverProps>;
export const ContextMenu: ComponentType<ContextMenuProps>;
export const Menu: ComponentType<MenuProps>;
export const MenuItem: ComponentType<MenuItemProps>;
export const Drawer: ComponentType<DrawerProps>;
export const DrawerPanel: ComponentType<DrawerPanelProps>;
export const CodeView: ComponentType<CodeViewProps>;
export const LogView: ComponentType<LogViewProps>;
export const Video: ComponentType<VideoProps>;
export const Chart: ComponentType<ChartProps>;

/** Every tag name, in catalogue order. */
export const tags: string[];

/** Every tag, as a host component React can create. */
export const components: Record<string, string>;

/** Whether a tag accepts child nodes. */
export function acceptsChildren(tag: string): boolean;

/** Every prop and handler name a component accepts. */
export const vocabulary: { props: string[]; handlers: string[] };

export const SOCKET: string;
export const PROTOCOL: number;

export type Topic = 'containers' | 'images' | 'volumes' | 'networks' | 'terminal' | 'extensions';
export type Division = 'beside' | 'below';
export interface WorkspaceInfo { name: string; architecture: string; image: string }
export interface WorkspaceState extends WorkspaceInfo { running: boolean; current: boolean }
export interface ContainerSummary { id: string; name: string; image: string; state: string; created: number }
export interface ImageSummary { id: string; reference: string; size: number; created: number }
export interface PaneSummary {
  slot: string;
  working_directory: string | null;
  command: string | null;
  occupant: 'terminal' | 'surface';
}
export interface TabSummary { id: string; title: string; panes: PaneSummary[] }
export interface PaneText { slot: string; lines: string[]; truncated: boolean }
export interface FileEntry { path: string; directory: boolean; size: number }
export interface VolumeSummary { name: string; driver: string; size: number }
export interface NetworkSummary { name: string; driver: string; scope: string }

export class ExtensionError extends Error {
  readonly kind: 'denied' | 'absent' | 'conflict' | 'failed' | 'unsupported';
  readonly capability?: string;
}

export interface ConnectOptions {
  path?: string;
  pendingLimit?: number;
  timeout?: number;
  onRows?: (request: unknown, channel: number) => void;
  onReply?: (reply: unknown) => void;
  onEvent?: (event: unknown, channel: number) => void;
}

export class Session {
  static connect(path?: string, handlers?: ConnectOptions): Promise<Session>;
  readonly ready: Promise<void>;
  readonly granted: readonly string[];
  call(method: string, params?: unknown): Promise<unknown>;
  answer(channel: number, window: unknown): void;
  onEvent(listener: (event: unknown, channel: number) => void): () => boolean;
  close(): void;
}

export function connect(options?: ConnectOptions): Promise<Session>;

export interface WorkspaceApi {
  info(): Promise<WorkspaceInfo>;
  list(): Promise<WorkspaceState[]>;
  containers: {
    list(): Promise<ContainerSummary[]>;
    inspect(id: string): Promise<ContainerSummary>;
    create(image: string, name: string): Promise<string>;
    start(id: string): Promise<void>;
    stop(id: string): Promise<void>;
    remove(id: string): Promise<void>;
  };
  images: { list(): Promise<ImageSummary[]>; pull(reference: string): Promise<ImageSummary> };
  terminal: {
    tabs(): Promise<TabSummary[]>;
    openTab(title: string): Promise<string>;
    split(slot: string, division: Division): Promise<string>;
    spawn(slot: string, command: string[]): Promise<void>;
    read(slot: string, lines?: number): Promise<PaneText>;
    close(slot: string): Promise<void>;
    focus(slot: string): Promise<void>;
    ratio(slot: string, ratio: number): Promise<void>;
  };
  files: {
    list(path: string): Promise<FileEntry[]>;
    read(path: string): Promise<number[]>;
    write(path: string, contents: Iterable<number>): Promise<void>;
  };
}

export function workspace(session: Session): WorkspaceApi;
/** Current protocol coverage. Names under `unavailable` deliberately have no methods. */
export const protocolCoverage: Readonly<{
  available: Readonly<{
    workspace: readonly ('info' | 'list')[];
    containers: readonly ('list' | 'inspect' | 'create' | 'start' | 'stop' | 'remove')[];
    images: readonly ('list' | 'pull')[];
    terminal: readonly ('tabs' | 'openTab' | 'split' | 'spawn' | 'read' | 'close' | 'focus' | 'ratio')[];
    files: readonly ('list' | 'read' | 'write')[];
    interfaceEvents: readonly ('invoke' | 'submit' | 'change' | 'select')[];
  }>;
  unavailable: Readonly<{
    workspace: readonly ('create' | 'delete' | 'updateConfiguration' | 'start' | 'stop' | 'restart')[];
    containers: readonly ('processes' | 'exec' | 'logs' | 'pause' | 'unpause' | 'restart' | 'kill')[];
    terminal: readonly ('writeInput' | 'resizeGrid' | 'switchOccupant' | 'paneProviders')[];
    events: readonly ('hostSnapshots' | 'keyboard' | 'focus' | 'pointer' | 'drag' | 'drop')[];
  }>;
}>;

export function render(
  element: ReactNode,
  session: Session,
  options?: { title?: string },
): { update(next: ReactNode): void; close(): void };

export function deliver(session: Session, payload: unknown): boolean;
